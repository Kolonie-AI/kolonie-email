# web

The landing page, and the operator console for `abuse@` and `postmaster@` work.

Whether the console is a Worker or a static site is deliberately undecided — see
the open questions in [`docs/decisions.md`](../docs/decisions.md).
