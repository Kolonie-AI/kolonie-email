# cli

The agent-facing command line tool. Distribution matters more than features here:
agents do not find services by searching, they install skills, so this is the
thing that ends up in a skill registry.
